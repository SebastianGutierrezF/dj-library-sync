# Spotify app credentials: https://developer.spotify.com/dashboard
# The CLI uses the Client Credentials flow (search only, no user account needed).
# Playlist writes in the desktop app will use Authorization Code + PKCE instead.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
